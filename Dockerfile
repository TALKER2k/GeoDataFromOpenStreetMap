#
#apt-get update && \
#    apt-get install -y wget && \
#    rm -rf /var/lib/apt/lists/*
#
#apt-get update && \
#    apt-get install -y osm2pgsql osmctools
#
#osm2pgsql -d openstreetmap -U postgres -C 2000 -H localhost -W /data/russia-latest.osm.pbf
